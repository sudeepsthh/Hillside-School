import React from 'react';

// This component has been replaced by NoticeBoard.tsx
// Keeping it as a null render to prevent build errors if referenced elsewhere
const NewsSection: React.FC = () => {
  return null;
};

export default NewsSection;